---
interface Props {
    image1: string;
    image2: string;
    title: string;
    body: string;
    href: string;
}

const { image1, image2, href, title, body } = Astro.props;
---

<a href={href} class="group relative block rounded-xl overflow-hidden">
    <div class="relative h-[350px] sm:h-[450px]">
        <img
            src={image1}
            alt={title}
            class="absolute inset-0 h-full w-full object-cover opacity-100 group-hover:opacity-0 transition-opacity"
        />

        <img
            src={image2}
            alt={title}
            class="absolute inset-0 h-full w-full object-cover opacity-0 group-hover:opacity-100 transition-opacity"
        />
    </div>

    <div class="absolute inset-0 flex flex-col items-start justify-end p-6">
        <h3 class="text-xl font-medium text-white">{title}</h3>

        <p class="mt-1.5 text-pretty text-xs text-white">
            {body}
        </p>

        <span
            class="mt-3 inline-block uppercase tracking-wide rounded bg-cyan-400 px-4 py-3 text-sm font-medium text-cyan-950 shadow hover:bg-cyan-700 hover:text-cyan-100 focus:outline-none focus:ring active:bg-cyan-400 sm:w-auto transition-colors"
        >
            Ver más detalles
        </span>
    </div>
</a>
